<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.polybooks">
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PolyBooks">
        <activity android:name=".ScanBarcode"
            android:parentActivityName=".AddSale"></activity>
        <activity android:name=".FillSale"
            android:parentActivityName=".AddSale"/>
        <activity android:name=".AddSale"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".DummyDatabaseActivity"></activity>
        <activity android:name=".FirebaseActivity"></activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>